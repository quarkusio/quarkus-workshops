## How to build the docs

Run `mvn -Dquickly` to just generate a default doc with every flag enabled
Run `mvn -Dos=[linux|mac|windows|all]` to generate a set of variants for a single OS. That should take around 2-3m.
The default build will take around 10m.
